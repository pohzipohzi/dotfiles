snippet dfr "defer recover"
	defer func() {
		if err := recover(); err != nil {
			${0:${VISUAL}}
		}
	}()

snippet ggm "go generate mockery"
	//go:generate mockery -name=${0:${VISUAL}} -case=underscore -inpkg
